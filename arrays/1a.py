numbers = [0 for i in range(6)] #Creating an array  List acts as an array.
for index in range(6): 
    numbers[i] = (int(input("Enter number: "))) 
avg = 0 
for i in range(len(numbers)): 
    avg = avg + i 
    print(numbers[-i-1]) 
print(f"Average: {avg/len(numbers)}") 

#Note: instead of using I for for loops, if looping through loops etc, name the variable index instead to make it more clear.
